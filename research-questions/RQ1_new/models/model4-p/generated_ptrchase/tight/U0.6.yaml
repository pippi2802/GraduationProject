# BASE manifest for one model-4-p cell -- identical design to model4's own
# job.yaml (see there for the full rationale), just its own namespace/node so
# it can run concurrently with model4-m on a different physical node.
---
apiVersion: rt.resource.example.com/v1alpha1
kind: RtClaimParameters
metadata: { namespace: rq1-model4-p, name: "model4-p-tight-u60-params" }
# requestedCpus pins the pair directly to two distinct physical cores (1 and
# 3 -- siblings are (0,1)/(2,3) on this hardware, cpu0 stays excluded since
# it's KEEP_CPU). Same fix as model4-m 2026-08-12: replaces the old
# delete/recreate-until-landed retry loop, validated end-to-end there
# (driver correctly allocates [1,3], containerd/cgroup/run_job.sh's own
# verification all agree -- see that day's backlog for the three-bug chain
# this uncovered and fixed).
spec: { count: 2, runtime: 6000, period: 10000, requestedCpus: [1, 3] }
---
apiVersion: resource.k8s.io/v1alpha2
kind: ResourceClaimTemplate
metadata: { namespace: rq1-model4-p, name: "model4-p-tight-u60-claim" }
spec:
  spec:
    resourceClassName: rt.example.com
    parametersRef: { apiGroup: rt.resource.example.com, kind: RtClaimParameters, name: "model4-p-tight-u60-params" }
---
apiVersion: v1
kind: Pod
metadata:
  namespace: rq1-model4-p
  name: "model4-p-tight-u60"
  labels: { app: model4-p, role: target }
spec:
  restartPolicy: Never
  nodeSelector: { experiment-model: model4-p }
  tolerations: [{ operator: Exists }]
  containers:
    - name: probe
      image: pippina2/rq1-probe:v3
      imagePullPolicy: IfNotPresent
      command: ["/bin/bash","-c"]
      args:
        - |
          set -e; mkdir -p /results; rm -f /results/jobs.csv || true
          # CORRECTED 2026-08-12: the driver (dra-rt-driver's WriteCgroupToCDI,
          # via containerd's own ExtractCDI which mirrors the same convention)
          # ALWAYS hyphen-joins the claimed cpu list as a plain delimiter --
          # never a range, whether the ids are consecutive or not. The
          # original 2026-08-09 fix ("3-2" broke taskset/matmul) correctly
          # spotted the symptom but wrongly diagnosed it as a reversed RANGE;
          # it's not a range at all, so seq-expanding "1-3" into {1,2,3} was
          # itself a bug -- silently three cpus instead of the two actually
          # allocated. Every past pair happened to be consecutive, where
          # "reversed range" and "hyphen-delimited pair" normalize to the same
          # result by coincidence; requestedCpus letting callers pick genuinely
          # non-consecutive, physically-separated pairs (e.g. 1,3) exposed it.
          # Treat hyphens as plain delimiters, same as commas -- never a range.
          if [ -n "$RT_CPUSET" ]; then
            RT_CPUSET=$(echo "$RT_CPUSET" | tr '-' ',' | tr ',' '\n' | grep -v '^$' | sort -n | paste -sd, -)
            PIN="taskset -c $RT_CPUSET"; CPU="--threads 2 --cpu-list env"
          else
            PIN=""; CPU="--threads 2 --cpu-list 0,1"
          fi
          exec $PIN /usr/local/bin/matmul --kind ptrchase --buf-kb 131072 --M 48 --K 71 --period-us 10000 \
            --n-jobs 5000 --warmup 200 --priority 90 --seed 20260713 $CPU \
            --logfile /results/jobs.csv
      securityContext: { capabilities: { add: ["SYS_NICE","IPC_LOCK"] } }
      resources: { claims: [{ name: rtcpu }] }
      volumeMounts: [{ name: results, mountPath: /results }]
  resourceClaims:
    - { name: rtcpu, source: { resourceClaimTemplateName: "model4-p-tight-u60-claim" } }
  volumes:
    - name: results
      hostPath: { path: /var/lib/rq1/model4-p/tight/U0.6/target, type: DirectoryOrCreate }
