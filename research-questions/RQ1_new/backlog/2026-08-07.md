# Backlog — 2026-08-07

## Done today

**model3 (base) ptrchase — stale-manifest fix carried over from yesterday**
- Base model3's `generated_ptrchase/_intf/` had never been regenerated after yesterday's `IPC_LOCK` fix to `generate_yaml.py` — only w3's had. Regenerated; confirmed the interferer capability line is now present across all four arms (model3, w2, w3, w4).

**Root-caused and fixed the recurring `StartError` (`cpu.rt_runtime_us: invalid argument`)**
- Traced the actual `rt-k8s` source (runc fork, `dra-rt-driver`, and the HCBS kernel patch itself, all at `~/Documents/rt-k8s/`) instead of reasoning from docs alone.
- Confirmed via the real kernel source (`kernel/sched/rt.c`'s `tg_rt_schedulable`/`walk_tg_tree`, `kernel/sched/core.c`'s `cpu_rt_runtime_write`) that admission is a live, atomic tree-walk over real children, serialized by the kernel's own `rt_constraints_mutex` — not dependent on any userspace-maintained running total.
- Found the real bug in the *active* code path, `runc/libcontainer/cgroups/fs2/cpu.go` (cgroup v2 — not the v1 `fs/cpu.go` I initially misread): `childrenSum`'s `preserveFloor=true` was applied uniformly to every ancestor level, when only `kubepods.slice` (the level `rt-budget-seed.sh` actually pre-seeds) should ever keep a floor. The QoS slice (besteffort) inherited that same floor, so a deleted pod's contribution was never released — a permanent, monotonically-growing leak, not a race.
- Fix: one-line change (`preserveFloor: i == len(chain)-1` instead of hardcoded `true`). Verified correctness by tracing the exact kernel admission logic for both the shrink case (safe — kubepods' own check substitutes the *proposed* smaller besteffort value, never rejected by a parent) and the grow case (unaffected, still correct for a first pod).
- Independently found the project's own pre-existing, never-built driver-repo docs (`doc/fix_budget_einval.md`, `doc/runbook-rt-budget-einval.md`) already describing this exact design ("besteffort self-heals, kubepods keeps a floor") as "the fixed runc" — confirming convergence with prior intent that had never actually been implemented in the shipped code.

**Deployed a second, separate driver fix (the user's own "v0.1.3")**
- Found already-written, never-built: a NAS-cache refresh (`d.nasclient.Get(ctx)`) before CDI generation in `driver.go`, fixing the transient `rtCDIDevices is nil or incomplete` race.
- Built `pippina2/dra-rt-driver:v0.1.3`, pushed, deployed to both the controller Deployment and kubeletplugin DaemonSet, verified via image digest (not just rollout status) that the real build was running on every pod.

**Deployed the runc fix and verified end-to-end, live**
- Rebuilt + installed on `rt-cluster-worker-1`; restarted containerd + kubelet.
- Manually created/deleted synthetic RT pods, watching `cpu.rt_runtime_us` directly: confirmed the leak no longer accumulates, self-heals on the *next* creation (not proactively on delete — a known, safe characteristic, unbounded-vs-bounded staleness).
- Diagnosed one real but narrow edge case along the way: a manual test with only a 5s gap between delete and create hit a genuine cgroup-directory cleanup lag (K8s API considers a pod gone before the node actually removes its cgroup), causing a transient double-count. Confirmed `run_job.sh`'s existing 12s inter-cell sleep already covers this window.
- Decisive test: reran the exact `model3 tight U0.9` cell that failed with `StartError` earlier today, through the real harness (`run_job.sh`, not a synthetic poke) — succeeded cleanly, `5000/5000` rows, real contention confirmed via the co-runner's own row count. Also confirmed placement-retry cycling (multiple delete+recreate attempts while landing on a cpu) no longer leaks per-attempt either — a second, independent confirmation.

## Tomorrow

1. **Roll the runc fix out to the remaining worker nodes** — only confirmed on `rt-cluster-worker-1` so far. Rebuild + install + restart containerd/kubelet on every other worker before trusting sweeps there; verify each with the same besteffort self-heal check before running anything real.
2. **Now actually execute yesterday's data backlog** (infra work ate all of today, none of this happened yet):
   - Cancel all ptrchase results except round3 for w2 and w4; promote those to round1.
   - Re-run all rounds of ptrchase (model3, w2, w3, w4) now that both the `IPC_LOCK` fix and today's leak fix are in place.
   - Re-run all rounds of model3 (base) and model3-w2 matmul, fresh.
3. Once the redo is done and a node's free: **model4**, matmul then ptrchase (`IMAGE=pippina2/rq1-probe:v3 ./build.sh` first).
