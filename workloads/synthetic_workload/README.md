# KubeDeadline Synthetic Workload

This type of workloads are inspired by the authors experiments which consists in a set of real-time periodic tasks that have been tested on the schedulabilty with CARTS.

Since CARTS assumes that the workload run on the physical hardware, it can be interested to use it and see where the schedulability guarantees break.